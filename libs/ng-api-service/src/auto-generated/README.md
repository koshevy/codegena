# Directory for auto-generated services

This directory is using for storing auto-generated service, based on templates in `src/api/templates`. Those services will be created by command:

```
npm run prepare
```

That command should be executed every time before tests and publishing.